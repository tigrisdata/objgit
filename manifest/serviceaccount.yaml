apiVersion: v1
kind: ServiceAccount
metadata:
  name: objgitd
  # To use EKS IRSA or GKE workload identity instead of a static Tigris
  # keypair: annotate this account, and delete the objgitd-tigris Secret
  # reference from deployment.yaml.
  #
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/objgitd
# annotations: {}
